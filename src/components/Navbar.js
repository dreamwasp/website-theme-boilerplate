import React from "react"

import navbar from "../styles/darkMode/navbar.module.scss"

export default () => {
  return (
    <div className={navbar.bar}>
      <div className={navbar.header}>this is the header</div>
    </div>
  )
}
